{
  "name": "gameboy-instruction-generator",
  "version": "1.0.0",
  "description": "Translates JSON of z80 instruction set into JS function signatures",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Chris Bell",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^14.14.21"
  }
}
